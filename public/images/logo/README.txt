LOGO IMAGE — drop your file here.

Expected filename:  logo.png   (transparent background preferred; .svg also
works — if you use logo.svg, update src/components/Logo.astro).

Recommended: a transparent PNG or SVG of the fleur-de-lis mark (or the full
logo lockup). It renders at ~36–40px tall in the header, so make sure it
reads well at small sizes. Aim for at least 2x that height (~80px) for crisp
display on retina screens.

NOTE: the header currently shows the logo image + the "Zion Classique Care"
wordmark text beside it. If your logo already includes the full name/wordmark,
tell me and I'll drop the text so it isn't duplicated.

A placeholder logo.png is already here so the header looks complete — just
overwrite it with your real logo (keep the same filename).
