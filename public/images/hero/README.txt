HERO IMAGE — drop your file here.

Expected filename:  hero.jpg   (you can also use hero.png — if so, update
the reference in src/components/Hero.astro from /images/hero/hero.jpg).

Recommended: a wide, calm facility/lifestyle photo, ~2000px wide, compressed
to keep it under ~400KB. It sits behind the headline at low opacity, so a
warm, not-too-busy image works best.

A placeholder hero.jpg is already here so the site looks complete — just
overwrite it with your real photo (keep the same filename).
